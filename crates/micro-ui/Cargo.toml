[package]
name = "micro-ui"
version = "0.1.0"
edition = "2024"

[dependencies]
indexmap = "2.6.0"
itertools = "0.15.0"
micro = { path = "../micro" }
tracy-client = { version = "0.19.0", default-features = false, features = [
	"broadcast",
	"callstack-inlines",
	"code-transfer",
	"context-switch-tracing",
	"sampling",
	"system-tracing",
] }
